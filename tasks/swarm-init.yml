---
# https://docs.docker.com/engine/swarm/swarm-tutorial/create-swarm/
- name: Docker swarm init
  ansible.builtin.command:
    cmd: >
      docker swarm init
      --advertise-addr {{ swarm_advertise_addr }}:{{ swarm_port }}
      --listen-address {{ swarm_listen_addr }}:{{ swarm_port }}
